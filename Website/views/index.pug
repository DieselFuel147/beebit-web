extends layout

block content
  h1= title
  p Welcome to #{title}
  form(action='/', method='post')
  input.navbar(name='data', type='text', placeholder='Enter data')
  input.navbar(type='submit', value='Get Data')